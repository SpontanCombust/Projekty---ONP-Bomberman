#ifndef __INTERACTION_HPP
#define __INTERACTION_HPP

typedef enum 
{
    I_KEYBOARD,
    I_MOUSE

} EInteractionType;

typedef enum
{
    IM_LEFT,
    IM_RIGHT,
    IM_MIDDLE,

    IK_

} EInteractionKey;

typedef struct SInteraction
{

} SInteraction;

#endif